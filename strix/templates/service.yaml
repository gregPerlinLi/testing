apiVersion: v1
kind: Service
metadata:
  name: {{ include "strix.fullname" . }}
  labels:
    {{- include "strix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "strix.selectorLabels" . | nindent 4 }}